import 'package:flutter/material.dart';

class UserPageCreateDesktop extends StatelessWidget {
  const UserPageCreateDesktop({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}